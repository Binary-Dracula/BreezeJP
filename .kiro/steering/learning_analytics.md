---
inclusion: always
---

# Learning Analytics Rules

> **Status: Frozen**
>
> 本文档定义 BreezeJP 中所有「学习统计 / 进度统计 / 完成率 / Dashboard 数值」的**唯一合法口径**。
>
> **任何不符合本文档的统计结果，均视为逻辑 Bug，而非 UI 或展示问题。**

---

## 1. Scope（适用范围）

本规则适用于：

* 假名（Kana）学习
* 单词（Word）学习
* Dashboard / Home 页面统计
* 成就系统（若有）
* 周报 / 月报 / 学习总结（若有）

不适用于：

* UI 展示样式
* 数据库存储结构
* SRS 算法内部参数

---

## 2. Learning Status Definitions（学习状态定义）

所有学习统计，**必须**基于统一的学习生命周期状态：

| Status     | Description              |
| ---------- | ------------------------ |
| `seen`     | 已曝光。用户看过内容，但未进入 SRS 学习阶段 |
| `learning` | SRS 学习中。用户已开始系统性学习       |
| `mastered` | 已掌握。用户已完成学习并退出 SRS       |
| `ignored`  | 已忽略。用户明确选择不学习            |

> 说明：
>
> * **数据库中不存在记录 ≠ `seen`**
> * `seen` 是一个**已进入学习生命周期但未承诺学习**的状态
> * `ignored` 是**路径控制状态，不是学习成果**

---

## 3. Learned Count（已学习数量）

### 定义

> **已学习数量 = 用户已进入或完成有效学习阶段的内容数量**

### 统计口径

```text
learning_status IN (learning, mastered)
```

### 明确排除

```text
learning_status IN (seen, ignored)
```

### 解释

* `learning`：正在学，算学习成果的一部分
* `mastered`：已完成学习，必须计入
* `seen`：仅曝光，不算学习
* `ignored`：明确拒绝学习，不算学习

---

## 4. Mastered Count（已掌握数量）

### 定义

> **已掌握数量 = 用户明确完成学习的内容数量**

### 统计口径

```text
learning_status = mastered
```

### 说明

* `mastered` 是学习生命周期的**终态**
* 不再参与 SRS
* 必须计入长期学习成果

---

## 5. Ignored Count（忽略数量）【辅助指标】

### 定义

> 用户明确选择不学习的内容数量

### 统计口径

```text
learning_status = ignored
```

### 使用限制

* ❌ 不计入任何学习成果指标
* ❌ 不参与完成率、掌握率计算
* ✅ 仅用于：

  * 个性化推荐
  * 内容过滤
  * 高级用户分析

---

## 6. Completion Rate（完成率）

### 定义

> 学习完成率反映「开始学习的内容中，有多少已完成」

### 合法计算公式

```text
completion_rate = mastered / (learning + mastered)
```

### 明确禁止

* ❌ 将 `seen` 计入分母
* ❌ 将 `ignored` 计入分母
* ❌ 使用「总内容数」作为分母

---

## 7. SRS Participation Rules（SRS 参与规则）

| Status     | 是否进入 SRS |
| ---------- | -------- |
| `seen`     | ❌        |
| `learning` | ✅        |
| `mastered` | ❌        |
| `ignored`  | ❌        |

> 说明：
> SRS 仅对 **learning 阶段** 的内容负责，
> 任何其它状态进入 SRS 都属于严重逻辑错误。

---

## 8. Prohibitions（明确禁止事项）

以下行为**被明确禁止**：

* 将 `seen` 计入任何学习成果统计
* 将 `ignored` 计入任何学习成果或完成率
* 使用 UI 层自由解释学习统计含义
* 在 Analytics / Query 中绕过本规则自行定义口径
* 为“让数字好看”而修改统计定义

---

## 9. Change Policy（变更策略）

* 本文件为 **Freeze 文档**
* 任何修改：

  * 必须同时修改：

    * 文档
    * Analytics 实现
    * Dashboard 解释文案（如有）
* 不允许：

  * 私下修改统计 SQL
  * 仅在 UI 层做“补救性解释”

---

## 10. Notes（备注）

* 本规则对 **Kana / Word 完全一致**
* 后续新增学习类型（如 Grammar）**必须复用本规则**
* 若某学习类型需要特殊统计，必须单独立 Freeze 文档

---

### 🔒 最后一条（强烈建议原样保留）

> **When analytics data feels wrong, check this document first.
> If analytics violates this document, it is a bug.**

---